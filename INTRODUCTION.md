# FreeLang fx2 — Lisp 스타일 함수 조립형 컴파일 언어

## 한 문장 정의

**Lisp 문법으로 쓰고, C 컴파일로 실행하는 네이티브 바이너리 언어.**

---

## 컴파일 파이프라인

```
소스 코드                변환                 네이티브 실행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

program.fl
   ↓
[문법 파싱 & 시맨틱 검사]
   ↓
cgc-bin (컴파일러)
   ↓
program.c
   ↓
gcc -o program program.c
   ↓
./program (ELF 바이너리)
   ↓
결과 출력
```

---

## 핵심 특징

### 표현력 (Syntax)
- **Lisp 스타일**: `(fn [$x] (+ $x 1))`, `(map f $list)`, `(reduce + 0 $nums)`
- **함수 조립형**: 고차함수, 클로저, 함수 합성 지원
- **마크로**: `->`, `->>` (threading macro)

### 실행 방식 (Runtime)
- **컴파일형**: 실행 전에 C → 바이너리로 변환
- **해석기 불필요**: 배포는 단일 ELF 파일만 필요
- **성능**: 순수 C 레벨의 속도 (JavaScript 대비 50~330배)

### 생태계
- **빌드 도구**: `fl-build.sh` (자동화)
- **패키지 시스템**: `fx.toml` (선언식)
- **검증**: `verify.sh` (컴파일 전 사전 검사)

---

## 장점

| 항목 | 설명 |
|------|------|
| **표현력** | 함수 합성, 고차함수, 재사용이 쉬움 |
| **배포** | 네이티브 바이너리 (해석기 불필요) |
| **성능** | C 레벨의 실행 속도 |
| **명확성** | 컴파일 파이프라인이 선형적 |
| **자동화** | 빌드, 검증, 패키징이 단순 |

---

## 제약사항

| 항목 | 설명 |
|------|------|
| **구현 난도** | 컴파일러·런타임 설계가 복잡 |
| **디버깅** | 소스 오류가 C/gcc 오류로 번질 수 있음 |
| **사양 안정성** | 언어 의미(클로저, truthiness)가 중요 |
| **학습 곡선** | Lisp 문법 + 컴파일 파이프라인 병행 |

---

## 언어 분류

| 항목 | 분류 |
|------|------|
| **문법** | Lisp 계열 (함수형) |
| **실행** | 컴파일형 (네이티브) |
| **패러다임** | 함수형 + 절차형 |
| **타입** | 동적 타입 |
| **메모리** | GC (자동 메모리 관리) |

---

## 시작하기

### 1. 간단한 프로그램

```lisp
(define PI 3.14159)

(defn circle-area [$r]
  (* PI (* $r $r)))

(circle-area 5)  ;; 78.539
```

### 2. 빌드

```bash
bash fl-build.sh program.fl program-bin
./program-bin
```

### 3. 컬렉션 처리

```lisp
(define $nums (list 1 2 3 4 5))

(filter (fn [$x] (> $x 2)) $nums)
;; [3 4 5]

(map (fn [$x] (* $x 2)) $nums)
;; [2 4 6]

(reduce (fn [$acc $x] (+ $acc $x)) 0 $nums)
;; 15
```

---

## 정식 문서

- **[SPEC.md](./SPEC.md)** — 완전한 언어 사양
- **[RELEASE-GATE.md](./RELEASE-GATE.md)** — 릴리즈 체크리스트
- **[spec/conformance.fl](./spec/conformance.fl)** — 기능 검증 테스트

---

## 버전

| 버전 | 릴리즈 | 주요 기능 |
|------|--------|----------|
| v2.0 | 2026-06-22 | 정식 사양 공개 |
| v1.1 | 2026-06-14 | `->` / `->>` 추가 |
| v1.0 | 2026-06-01 | 초기 릴리즈 |

---

**기록이 증명이다.**
