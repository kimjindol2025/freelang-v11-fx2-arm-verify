# AFJ DB verification smoke\n\nThis fixture declares the existing AFJ DB v2 AFJ CLI. `fl verify` runs its quick connect/build, temporary append, read-key, value comparison, hash-chain verify, and cleanup sequence. `fl verify --full` additionally runs replay, snapshot, and restore.\n\nThe database root is a relative manifest path; verification data is created under a disposable `/tmp/fl-verify-afj-db-*` namespace and removed in a finally path.\n